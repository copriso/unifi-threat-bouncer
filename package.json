{
  "name": "unifi-threat-crowdsec",
  "version": "1.0.0",
  "description": "Forward Unifi threats to Crowdsec",
  "main": "dest/index.js",
  "scripts": {
    "dev": "ts-node-dev --poll ./src/index.ts",
    "build": "npx prisma generate && rimraf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.2",
    "prisma": "^6.1.0",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "crowdsec-client": "^1.0.0"
  }
}
